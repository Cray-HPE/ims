
@Library('dst-shared@release/shasta-1.4') _

dockerBuildPipeline {
    app = "ims-service"
    name = "cms-ims-service"
    description = "Cray Management Image Management Service"
    repository = "cray"
    imagePrefix = "cray"
    dockerfile = "Dockerfile.service"
    product = "csm"
    enableSonar = true
    receiveEvent = ["IMS"]
}
